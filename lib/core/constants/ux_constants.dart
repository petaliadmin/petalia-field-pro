/// UX constants optimized for field workers (gloved hands, harsh sun).
class UxConstants {
  UxConstants._();

  /// Minimum touch target for field conditions (Material default is 48dp).
  static const double kMinTouchTarget = 48.0;

  /// Preferred touch target for primary actions.
  static const double kLargeTouchTarget = 56.0;

  /// Minimum font size for outdoor readability.
  static const double kMinFieldFontSize = 13.0;
}
