/**
 * Environnement de développement (par défaut).
 * NB : ne PAS committer d'IP de production ici — utiliser environment.prod.ts
 * (substitué automatiquement par angular.json en mode production).
 */
export const environment = {
  production: false,
  apiUrl: 'http://52.73.53.182:3000',
};
